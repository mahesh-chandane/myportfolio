import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class PortfolioServiceService {
  saveUser: any;
  // saveUser(user_name: string, user_email: string, message: string) {
  //   throw new Error('Method not implemented.');
  // }

  constructor() { }
}
